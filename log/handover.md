# 2026-01-14 任務交接 (SynthBlockly)

## 當前進度
- **系統熱鍵**：完成 `Ctrl+Enter` (執行) 與 `Esc` (停止) 的全域監聽。
- **ADSR 視覺化**：重構為線性時間映射，解決了 A=0 或 R=0 的顯示誤差，動畫光點已同步。
- **Toolbox 重構**：合併「音源」與「音源控制」分類，調整 `PC Keyboard` 順序，統一積木顏色。
- **取樣器視覺區隔**：`sb_create_sampler_instrument` 改為深綠色 (`#388E3C`) 以利區分。

## 下一步建議
1. **補全文件**：`public/docs/` 下的 `custom_sampler_readme` 需要詳細撰寫如何編寫 Sample Map JSON。
2. **效果器參數優化**：目前的 `sb_setup_effect` 參數較多，可考慮是否也要像音源建立積木一樣，加入 Mutator 根據效果器類型動態顯示參數。
3. **移動端熱鍵建議**：考慮在觸控介面上增加更明顯的緊急停止按鈕，因為行動裝置無法使用 `Esc`。

## 待處理事項 (Todo)
- [ ] 檢查所有新積木的 `HelpUrl` 是否皆指向正確的 Vite 靜態路徑。
- [ ] 測試多個取樣器同時發聲時，ADSR 視覺化光點的切換邏輯（目前採最後觸發者優先）。
