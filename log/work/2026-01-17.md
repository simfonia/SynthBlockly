# 2026-01-17 工作日誌

## 今日進度
1. **修正 `sb_set_adsr` 名稱匹配問題**：
   - 解決了當使用者建立與系統同名樂器（如 `DefaultSynth`）時，ADSR 設定無法正確套用的問題。
   - 調整生成器邏輯，優先搜尋使用者自訂樂器列表。
2. **優化預設音色參數**：
   - 將全域預設 Release (R) 時間從 1.0s 調整為 0.5s，使音色更俐落。
3. **範例教材優化**：
   - 更新 `01_Melody and Chord.xml`、`02_rock.xml`、`03_Chord.xml` 的教學註解。
   - 在 `03_Chord.xml` 加入 Compressor 示範，解決和弦破音問題並提供教學對比。

## 關鍵技術細節 (踩過的坑)
- **名稱優先權**：系統預設物件（如 `synth`）與使用者物件（`instruments['DefaultSynth']`）重名時，應以使用者建立的優先。
- **UI 同步**：`updateADSR` 應在特定音源被修改時主動檢查是否需要更新圖形，不應僅依賴 `currentInstrumentName` 的嚴格匹配。
